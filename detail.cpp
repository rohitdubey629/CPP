using namespace std;

#include <iostream>

int main(int argc, char const *argv[])
{
    string name, course, add;
    cout << "Enter name course address";
    cin >> name >> course >> add;

    cout << "Name : " << name << "\nCourse : " << course << "\nAddress : " << add;
    return 0;
}
